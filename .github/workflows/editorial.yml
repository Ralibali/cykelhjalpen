name: Editorial publication
on:
  pull_request:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  editorial:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.0
      - run: bun install --frozen-lockfile
      - run: bun run editorial:check
      - run: bun run typecheck
      - name: Lint article publishing files
        run: bunx eslint src/components/EditorialBody.tsx src/pages/cykelhjalpen/EditorialBlog.tsx src/components/cykelhjalpen/CykelFooter.tsx scripts/editorial-html.mjs scripts/editorial-check.mjs scripts/generate-editorial.mjs scripts/verify-editorial-build.mjs scripts/verify-editorial-test-results.mjs
      - name: Run the full test suite
        run: bun run test --reporter=json --outputFile=editorial-test-results.json
        continue-on-error: true
      - name: Reject regressions outside the exact existing schema baseline
        run: node scripts/verify-editorial-test-results.mjs
      - run: bun run build
      - run: node scripts/verify-editorial-build.mjs
